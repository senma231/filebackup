# Windows文档扫描与上传系统 - 实施计划

## 1. 项目架构设计

### 1.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        Windows Agent (Go)                         │
├─────────────────┬─────────────────┬─────────────────┬─────────────┤
│   文件扫描模块   │   文件上传模块   │   心跳监控模块   │  配置管理模块 │
│  - 文件发现      │  - SFTP连接     │  - 状态上报     │  - 配置获取   │
│  - 文件过滤      │  - 断点续传     │  - 系统信息     │  - 配置解析   │
│  - 文件打包      │  - 进度跟踪     │  - 错误处理     │  - 配置更新   │
└─────────────────┴─────────────────┴─────────────────┴─────────────┘
                              │
                              │ HTTP/HTTPS
                              │
┌─────────────────────────────────────────────────────────────────┐
│                        Server端 (Go + Gin)                        │
├─────────────────┬─────────────────┬─────────────────┬─────────────┤
│   Agent管理模块  │   配置管理模块   │   文件管理模块   │  Web管理界面 │
│  - 注册认证      │  - 配置存储     │  - 上传记录     │  - 仪表板    │
│  - 状态监控      │  - 配置分发     │  - 存储管理     │  - Agent管理│
│  - 信息管理      │  - 版本控制     │  - 文件检索     │  - 统计分析  │
└─────────────────┴─────────────────┴─────────────────┴─────────────┘
                              │
                              │ SFTP
                              │
┌─────────────────────────────────────────────────────────────────┐
│                        存储层                                     │
│  - SFTP服务器 (VPS/云服务器)                                      │
│  - 本地文件系统                                                   │
│  - 云存储 (预留接口)                                              │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 技术架构详图

#### 1.2.1 Windows Agent架构 (Go)

```
┌─────────────────────────────────────────┐
│              Agent 主程序                │
│  (main.go - 程序入口与初始化)            │
├─────────────────────────────────────────┤
│              核心服务层                  │
│  ┌─────────────┬─────────────┬────────┐ │
│  │ ConfigMgr   │ ScanEngine  │Upload  │ │
│  │ 配置管理器   │ 扫描引擎     │上传器  │ │
│  └─────────────┴─────────────┴────────┘ │
│  ┌─────────────┬─────────────┬────────┐ │
│  │HeartbeatSvc │ LogService  │Health  │ │
│  │心跳服务     │ 日志服务     │健康检查│ │
│  └─────────────┴─────────────┴────────┘ │
├─────────────────────────────────────────┤
│              外部依赖层                  │
│  - SFTP客户端 (github.com/pkg/sftp)     │
│  - 文件监控 (github.com/fsnotify/fsnotify)│
│  - JSON处理 (encoding/json)             │
│  - HTTP客户端 (net/http)                │
│  - Windows服务 (golang.org/x/sys/windows)│
├─────────────────────────────────────────┤
│              系统接口层                  │
│  - 文件系统API                           │
│  - 网络通信API                           │
│  - Windows服务API                        │
└─────────────────────────────────────────┘
```

**核心组件说明**:

1. **ConfigMgr (配置管理器)**
   - 功能：管理Agent配置文件
   - 文件格式：JSON
   - 支持热更新
   - 配置加密存储

2. **ScanEngine (扫描引擎)**
   - 功能：扫描本地文档文件
   - 支持多线程扫描
   - 文件类型过滤
   - 增量扫描

3. **Upload (上传器)**
   - 功能：文件上传到SFTP
   - 支持断点续传
   - 压缩打包
   - 进度跟踪

4. **HeartbeatSvc (心跳服务)**
   - 功能：定期向Server发送心跳
   - 上报Agent状态
   - 获取配置更新

5. **LogService (日志服务)**
   - 功能：本地日志记录
   - 日志轮转
   - 多级别日志

6. **Health (健康检查)**
   - 功能：监控系统状态
   - 资源使用监控
   - 自动重启机制

#### 1.2.2 Server端架构 (Go + Gin)

```
┌─────────────────────────────────────────┐
│            Web服务器 (Gin)              │
│  - HTTP/HTTPS服务                       │
│  - 路由管理                             │
│  - 中间件处理                           │
├─────────────────────────────────────────┤
│              业务逻辑层                  │
│  ┌─────────────┬─────────────┐         │
│  │AgentHandler │ConfigHandler│         │
│  │Agent处理器  │配置处理器   │         │
│  └─────────────┴─────────────┘         │
│  ┌─────────────┬─────────────┐         │
│  │FileHandler  │ReportHandler│         │
│  │文件处理器   │报告处理器   │         │
│  └─────────────┴─────────────┘         │
├─────────────────────────────────────────┤
│              数据访问层                  │
│  ┌─────────────────────────────────────┐ │
│  │         Repository 层                │ │
│  │  - AgentRepository                  │ │
│  │  - ConfigRepository                 │ │
│  │  - FileRepository                   │ │
│  │  - LogRepository                    │ │
│  └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│              数据存储层                  │
│  - SQLite数据库                         │
│  - SFTP连接池                           │
│  - 文件系统                             │
└─────────────────────────────────────────┘
```

**核心组件说明**:

1. **AgentHandler (Agent处理器)**
   - 处理Agent注册
   - 接收心跳数据
   - 管理Agent状态

2. **ConfigHandler (配置处理器)**
   - 配置CRUD操作
   - 配置版本管理
   - 配置分发

3. **FileHandler (文件处理器)**
   - 文件上传记录
   - 文件状态跟踪
   - 文件检索

4. **ReportHandler (报告处理器)**
   - 数据统计分析
   - 报表生成
   - 导出功能

5. **Repository 层**
   - 数据持久化
   - 缓存管理
   - 事务处理

## 2. 数据库设计

### 2.1 数据库表结构

#### 2.1.1 Agent表 (agents)

```sql
CREATE TABLE agents (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    agent_id VARCHAR(64) UNIQUE NOT NULL,           -- Agent唯一标识
    hostname VARCHAR(255) NOT NULL,                 -- 计算机名
    ip_address VARCHAR(45) NOT NULL,                -- IP地址
    os_version VARCHAR(255),                        -- 操作系统版本
    agent_version VARCHAR(50),                      -- Agent版本
    status VARCHAR(20) DEFAULT 'online',            -- 状态：online/offline
    last_heartbeat DATETIME,                        -- 最后心跳时间
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,  -- 创建时间
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP   -- 更新时间
);
```

#### 2.1.2 配置表 (configs)

```sql
CREATE TABLE configs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    config_name VARCHAR(255) NOT NULL,              -- 配置名称
    config_type VARCHAR(50) NOT NULL,               -- 配置类型：global/agent
    target_agent_id VARCHAR(64),                    -- 目标Agent ID (NULL表示全局配置)
    config_data TEXT NOT NULL,                      -- 配置内容 (JSON格式)
    version INTEGER DEFAULT 1,                      -- 配置版本
    is_active BOOLEAN DEFAULT 1,                    -- 是否激活
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (target_agent_id) REFERENCES agents(agent_id)
);
```

#### 2.1.3 文件上传记录表 (file_uploads)

```sql
CREATE TABLE file_uploads (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    agent_id VARCHAR(64) NOT NULL,                  -- Agent ID
    local_path VARCHAR(1024) NOT NULL,              -- 本地文件路径
    remote_path VARCHAR(1024) NOT NULL,             -- 远程文件路径
    file_name VARCHAR(255) NOT NULL,                -- 文件名
    file_size BIGINT NOT NULL,                      -- 文件大小(字节)
    file_type VARCHAR(50),                          -- 文件类型
    upload_status VARCHAR(20) DEFAULT 'pending',    -- 上传状态：pending/success/failed
    error_message TEXT,                             -- 错误信息
    upload_start_time DATETIME,                     -- 上传开始时间
    upload_end_time DATETIME,                       -- 上传结束时间
    retry_count INTEGER DEFAULT 0,                  -- 重试次数
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (agent_id) REFERENCES agents(agent_id)
);
```

#### 2.1.4 心跳日志表 (heartbeats)

```sql
CREATE TABLE heartbeats (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    agent_id VARCHAR(64) NOT NULL,                  -- Agent ID
    status VARCHAR(20) NOT NULL,                    -- 状态
    cpu_usage DECIMAL(5,2),                         -- CPU使用率
    memory_usage BIGINT,                            -- 内存使用(字节)
    disk_usage BIGINT,                              -- 磁盘使用(字节)
    scan_count INTEGER DEFAULT 0,                   -- 扫描文件数
    upload_count INTEGER DEFAULT 0,                 -- 上传文件数
    error_count INTEGER DEFAULT 0,                  -- 错误数
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,   -- 心跳时间
    FOREIGN KEY (agent_id) REFERENCES agents(agent_id)
);
```

#### 2.1.5 系统日志表 (system_logs)

```sql
CREATE TABLE system_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    log_level VARCHAR(20) NOT NULL,                 -- 日志级别：info/warn/error
    module VARCHAR(50) NOT NULL,                    -- 模块名：agent/config/file/upload
    message TEXT NOT NULL,                          -- 日志内容
    agent_id VARCHAR(64),                           -- 相关Agent ID (可选)
    stack_trace TEXT,                               -- 堆栈跟踪 (错误时)
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (agent_id) REFERENCES agents(agent_id)
);
```

#### 2.1.6 文件统计表 (file_stats)

```sql
CREATE TABLE file_stats (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    agent_id VARCHAR(64) NOT NULL,                  -- Agent ID
    stat_date DATE NOT NULL,                        -- 统计日期
    total_files INTEGER DEFAULT 0,                  -- 总文件数
    total_size BIGINT DEFAULT 0,                    -- 总大小
    doc_count INTEGER DEFAULT 0,                    -- Word文档数
    excel_count INTEGER DEFAULT 0,                  -- Excel文件数
    ppt_count INTEGER DEFAULT 0,                    -- PowerPoint文件数
    pdf_count INTEGER DEFAULT 0,                    -- PDF文件数
    txt_count INTEGER DEFAULT 0,                    -- 文本文件数
    uploaded_count INTEGER DEFAULT 0,               -- 已上传数
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (agent_id) REFERENCES agents(agent_id),
    UNIQUE(agent_id, stat_date)
);
```

### 2.2 数据库索引

```sql
-- Agent表索引
CREATE INDEX idx_agents_status ON agents(status);
CREATE INDEX idx_agents_last_heartbeat ON agents(last_heartbeat);

-- 配置表索引
CREATE INDEX idx_configs_type ON configs(config_type);
CREATE INDEX idx_configs_agent ON configs(target_agent_id);
CREATE INDEX idx_configs_active ON configs(is_active);

-- 文件上传记录表索引
CREATE INDEX idx_file_uploads_agent ON file_uploads(agent_id);
CREATE INDEX idx_file_uploads_status ON file_uploads(upload_status);
CREATE INDEX idx_file_uploads_created ON file_uploads(created_at);

-- 心跳日志表索引
CREATE INDEX idx_heartbeats_agent ON heartbeats(agent_id);
CREATE INDEX idx_heartbeats_timestamp ON heartbeats(timestamp);

-- 系统日志表索引
CREATE INDEX idx_system_logs_level ON system_logs(log_level);
CREATE INDEX idx_system_logs_agent ON system_logs(agent_id);
CREATE INDEX idx_system_logs_created ON system_logs(created_at);

-- 文件统计表索引
CREATE INDEX idx_file_stats_agent ON file_stats(agent_id);
CREATE INDEX idx_file_stats_date ON file_stats(stat_date);
```

## 3. API接口设计

### 3.1 Agent相关接口

#### 3.1.1 Agent注册
```
POST /api/v1/agents/register
Content-Type: application/json

Request Body:
{
    "agent_id": "string",       // Agent唯一标识
    "hostname": "string",       // 计算机名
    "ip_address": "string",     // IP地址
    "os_version": "string",     // 操作系统版本
    "agent_version": "string"   // Agent版本
}

Response:
{
    "code": 200,
    "message": "success",
    "data": {
        "token": "string",      // 认证令牌
        "config_id": 1          // 配置ID
    }
}
```

#### 3.1.2 心跳上报
```
POST /api/v1/agents/{agent_id}/heartbeat
Authorization: Bearer {token}
Content-Type: application/json

Request Body:
{
    "status": "online",
    "cpu_usage": 45.5,
    "memory_usage": 1024000,
    "disk_usage": 50000000,
    "scan_count": 100,
    "upload_count": 50,
    "error_count": 0
}

Response:
{
    "code": 200,
    "message": "success",
    "data": {
        "config_update": false,     // 是否需要更新配置
        "config_data": null         // 新的配置数据
    }
}
```

#### 3.1.3 获取配置
```
GET /api/v1/agents/{agent_id}/config
Authorization: Bearer {token}

Response:
{
    "code": 200,
    "message": "success",
    "data": {
        "scan_paths": ["/path/to/scan"],
        "file_types": [".doc", ".docx", ...],
        "exclude_patterns": ["*.log"],
        "sftp_config": {
            "host": "example.com",
            "port": 22,
            "username": "user",
            // 密码/密钥通过其他方式下发
        },
        "heartbeat_interval": 30,
        "max_file_size": 104857600,
        "compress_enabled": true
    }
}
```

### 3.2 文件上传相关接口

#### 3.2.1 上传进度上报
```
POST /api/v1/agents/{agent_id}/upload/progress
Authorization: Bearer {token}
Content-Type: application/json

Request Body:
{
    "file_name": "document.docx",
    "file_size": 1048576,
    "file_type": ".docx",
    "upload_progress": 50,    // 上传进度百分比
    "status": "uploading"     // uploading/success/failed
}

Response:
{
    "code": 200,
    "message": "success"
}
```

### 3.3 管理接口

#### 3.3.1 获取Agent列表
```
GET /api/v1/admin/agents?page=1&size=20&status=all

Response:
{
    "code": 200,
    "message": "success",
    "data": {
        "total": 100,
        "agents": [
            {
                "agent_id": "string",
                "hostname": "string",
                "ip_address": "string",
                "status": "online",
                "last_heartbeat": "2025-12-15T10:00:00Z",
                "agent_version": "1.0.0"
            }
        ]
    }
}
```

#### 3.3.2 获取文件上传记录
```
GET /api/v1/admin/files?agent_id={agent_id}&start_date=2025-12-01&end_date=2025-12-31&page=1&size=20

Response:
{
    "code": 200,
    "message": "success",
    "data": {
        "total": 500,
        "files": [
            {
                "file_name": "document.docx",
                "file_size": 1048576,
                "upload_status": "success",
                "upload_time": "2025-12-15T10:00:00Z",
                "remote_path": "/uploads/agent1/2025-12-15/document.docx"
            }
        ]
    }
}
```

#### 3.3.3 配置管理
```
GET /api/v1/admin/configs/{config_id}    // 获取配置
PUT /api/v1/admin/configs/{config_id}    // 更新配置
POST /api/v1/admin/configs               // 创建配置
DELETE /api/v1/admin/configs/{config_id} // 删除配置
```

## 4. 开发计划与时间线

### 4.1 项目阶段划分

#### 第一阶段：基础设施建设 (2周)
**目标**: 完成项目基础框架搭建

**任务列表**:
- [ ] 项目结构创建
  - Agent项目结构设计
  - Server项目结构设计
  - 公共库设计
- [ ] 开发环境搭建
  - Go开发环境配置
  - 数据库初始化脚本
  - Docker开发环境
- [ ] 核心工具库开发
  - 日志库封装
  - 配置管理库
  - HTTP客户端库
- [ ] 数据库设计与实现
  - 表结构创建
  - 数据库访问层实现
  - 基础CRUD操作

**交付物**:
- 项目代码框架
- 数据库初始化脚本
- 基础工具库
- 开发文档

#### 第二阶段：Agent核心功能开发 (3周)
**目标**: 完成Agent主要功能模块

**任务列表**:
- [ ] 配置管理模块
  - 配置文件读取与解析
  - 配置热更新机制
  - 配置加密存储
- [ ] 文件扫描模块
  - 文件系统遍历
  - 文件类型过滤
  - 文件信息提取
  - 增量扫描机制
- [ ] 文件上传模块
  - SFTP客户端实现
  - 文件传输功能
  - 断点续传实现
  - 压缩打包功能
- [ ] 心跳监控模块
  - 心跳定时器
  - 系统信息收集
  - 状态上报
- [ ] 主程序整合
  - 服务启动流程
  - 模块间协调
  - 错误处理

**交付物**:
- Agent可执行程序
- 配置文件模板
- 单元测试报告

#### 第三阶段：Server端API开发 (3周)
**目标**: 完成Server核心API服务

**任务列表**:
- [ ] Web框架搭建
  - Gin框架集成
  - 路由配置
  - 中间件开发
- [ ] Agent管理接口
  - Agent注册接口
  - 心跳接收接口
  - 状态管理
- [ ] 配置管理接口
  - 配置CRUD接口
  - 配置版本控制
  - 配置分发接口
- [ ] 文件管理接口
  - 上传记录接口
  - 文件状态查询
  - 文件检索接口
- [ ] 数据访问层
  - Repository实现
  - 事务管理
  - 缓存机制

**交付物**:
- Server可执行程序
- API文档
- 数据库设计文档

#### 第四阶段：Web管理界面开发 (2周)
**目标**: 完成Web前端界面

**任务列表**:
- [ ] 前端框架搭建
  - 技术选型（Vue.js/React）
  - 项目初始化
  - 路由配置
- [ ] 仪表板开发
  - 实时数据展示
  - 统计图表
  - 状态监控
- [ ] Agent管理页面
  - Agent列表
  - 详情查看
  - 操作面板
- [ ] 配置管理页面
  - 配置编辑
  - 配置预览
  - 版本历史
- [ ] 文件管理页面
  - 上传记录查看
  - 文件搜索
  - 统计报表

**交付物**:
- Web前端应用
- 用户操作手册
- 界面原型图

#### 第五阶段：系统集成与测试 (2周)
**目标**: 完成系统集成和全面测试

**任务列表**:
- [ ] 端到端测试
  - Agent-Server通信测试
  - 文件上传流程测试
  - 配置下发测试
- [ ] 性能测试
  - 并发Agent测试
  - 大文件上传测试
  - 内存泄漏测试
- [ ] 稳定性测试
  - 长时间运行测试
  - 异常情况测试
  - 恢复能力测试
- [ ] 安全测试
  - 认证授权测试
  - 数据加密验证
  - 漏洞扫描

**交付物**:
- 测试报告
- 性能基准报告
- 安全评估报告

#### 第六阶段：部署与交付 (1周)
**目标**: 完成生产环境部署

**任务列表**:
- [ ] 部署文档编写
  - 安装指南
  - 配置说明
  - 运维手册
- [ ] 打包与发布
  - Agent打包为单文件
  - Server容器化部署
  - 安装包制作
- [ ] 生产环境部署
  - 服务器配置
  - 数据库部署
  - 监控配置
- [ ] 用户培训
  - 操作培训
  - 常见问题解答

**交付物**:
- 部署包
- 部署文档
- 用户手册
- 培训材料

### 4.2 详细时间线

```
Week  1-2  : 基础设施建设
         ├─ 项目结构创建
         ├─ 环境搭建
         └─ 基础库开发

Week  3-5  : Agent核心功能开发
         ├─ 配置管理模块
         ├─ 文件扫描模块
         ├─ 文件上传模块
         └─ 心跳监控模块

Week  6-8  : Server端API开发
         ├─ Web框架搭建
         ├─ Agent管理接口
         ├─ 配置管理接口
         └─ 文件管理接口

Week  9-10 : Web管理界面开发
         ├─ 前端框架搭建
         ├─ 仪表板开发
         └─ 管理页面开发

Week 11-12 : 系统集成与测试
         ├─ 端到端测试
         ├─ 性能测试
         ├─ 稳定性测试
         └─ 安全测试

Week 13    : 部署与交付
         ├─ 部署文档
         ├─ 打包发布
         └─ 用户培训
```

### 4.3 里程碑节点

| 里程碑 | 时间点 | 交付物 | 验收标准 |
|--------|--------|--------|----------|
| M1 | Week 2 | 基础框架 | 代码结构清晰，基础库可用 |
| M2 | Week 5 | Agent原型 | Agent可独立运行，核心功能可用 |
| M3 | Week 8 | Server API | API接口完整，数据库操作正常 |
| M4 | Week 10 | Web界面 | 前端界面完整，功能可用 |
| M5 | Week 12 | 测试版本 | 通过所有测试用例 |
| M6 | Week 13 | 正式版本 | 完成部署文档，用户培训 |

## 5. 测试策略

### 5.1 测试分类

#### 5.1.1 单元测试
**覆盖率要求**: > 80%

**测试范围**:
- Agent各模块单元测试
- Server API单元测试
- 数据库操作单元测试
- 工具库单元测试

**测试工具**:
- Go: testing包, testify
- 前端: Jest, Vue Test Utils

#### 5.1.2 集成测试
**测试场景**:
- Agent与Server通信
- 文件上传流程
- 配置下发流程
- 数据库读写操作

**测试方法**:
- 使用Docker搭建测试环境
- 自动化测试脚本
- 模拟多Agent场景

#### 5.1.3 系统测试
**测试内容**:
- 端到端功能测试
- 兼容性测试（Windows 10/11）
- 性能压力测试
- 安全性测试

### 5.2 性能测试

#### 5.2.1 并发测试
- **50个Agent同时在线**: 稳定运行24小时
- **1000个文件并发上传**: 成功率 > 99%
- **Server内存使用**: < 500MB
- **Agent内存使用**: < 100MB

#### 5.2.2 大文件测试
- **文件大小**: 100MB - 1GB
- **上传速度**: 最低10MB/s
- **断点续传**: 支持中断恢复
- **压缩效率**: 压缩比 > 50%

### 5.3 安全测试

#### 5.3.1 认证测试
- Agent身份验证
- API访问控制
- Token过期处理

#### 5.3.2 数据安全
- 配置文件加密
- 敏感信息脱敏
- 传输加密验证

#### 5.3.3 漏洞扫描
- SQL注入测试
- XSS攻击测试
- 权限提升测试

## 6. 部署方案

### 6.1 Agent部署

#### 6.1.1 打包方式
- **单文件可执行**: 使用Go build -ldflags生成
- **体积优化**: UPX压缩，目标大小 < 10MB
- **依赖处理**: 静态编译，无外部依赖

#### 6.1.2 安装方式
- **静默安装**: 支持命令行参数安装
- **Windows服务**: 自动注册为系统服务
- **开机自启**: 默认启用，可配置禁用

#### 6.1.3 配置文件
- **默认配置**: 内置默认配置
- **外部配置**: 支持JSON/YAML配置文件
- **环境变量**: 支持部分配置通过环境变量设置

### 6.2 Server部署

#### 6.2.1 容器化部署
```yaml
# docker-compose.yml
version: '3.8'
services:
  server:
    image: doc-scanner-server:latest
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - DB_PATH=/app/data/database.db
      - SFTP_HOST=example.com
      - SFTP_PORT=22
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/ssl/certs
    depends_on:
      - server
```

#### 6.2.2 系统服务
```ini
# systemd服务文件
[Unit]
Description=Document Scanner Server
After=network.target

[Service]
Type=simple
User=app
ExecStart=/usr/local/bin/doc-scanner-server
WorkingDirectory=/opt/doc-scanner
Restart=always
RestartSec=10
Environment=DB_PATH=/opt/doc-scanner/data/database.db

[Install]
WantedBy=multi-user.target
```

#### 6.2.3 数据库部署
- **SQLite**: 单文件数据库，默认使用
- **备份策略**: 每日自动备份
- **数据迁移**: 提供升级脚本

### 6.3 SFTP服务器部署

#### 6.3.1 VPS部署
```bash
# 安装OpenSSH
sudo apt-get update
sudo apt-get install openssh-server

# 创建SFTP用户组
sudo groupadd sftponly

# 配置SSH
# /etc/ssh/sshd_config
Match Group sftponly
    ChrootDirectory /var/sftp
    ForceCommand internal-sftp
    AllowTcpForwarding no

# 重启SSH服务
sudo systemctl restart sshd
```

#### 6.3.2 云存储配置
- **S3配置**: 提供S3接口适配器
- **阿里云OSS**: 提供OSS接口适配器
- **腾讯云COS**: 提供COS接口适配器

### 6.4 监控与运维

#### 6.4.1 日志管理
- **日志级别**: DEBUG, INFO, WARN, ERROR
- **日志轮转**: 每日轮转，保留30天
- **日志收集**: 支持ELK Stack

#### 6.4.2 监控指标
- Agent在线率
- 文件上传成功率
- Server响应时间
- 数据库性能

#### 6.4.3 告警机制
- Agent离线告警
- 上传失败告警
- 磁盘空间告警
- 内存使用告警

## 7. 风险管控

### 7.1 技术风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| Go语言Windows兼容性问题 | 中 | 中 | 充分测试，使用成熟库 |
| 大文件上传超时 | 高 | 高 | 实现断点续传，增加重试 |
| 并发连接数限制 | 中 | 中 | 连接池管理，限流控制 |
| 内存泄漏 | 低 | 高 | 代码审查，内存监控 |
| 数据库锁竞争 | 中 | 中 | 优化SQL语句，使用索引 |

### 7.2 进度风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 需求变更 | 中 | 中 | 需求冻结，变更流程 |
| 技术难点攻克时间过长 | 低 | 高 | 技术预研，备用方案 |
| 测试不充分 | 中 | 高 | 自动化测试，增加测试时间 |
| 人员变动 | 低 | 中 | 代码文档化，知识分享 |

### 7.3 应对策略

1. **每周进度会议**: 评估进度，识别风险
2. **技术评审**: 关键技术点提前评审
3. **代码审查**: 所有代码强制审查
4. **自动化测试**: 提高测试覆盖率
5. **备份计划**: 关键节点制定Plan B

## 8. 质量保证

### 8.1 代码质量

#### 8.1.1 编码规范
- **Go语言规范**: 遵循Go官方代码规范
- **注释要求**: 公共API必须注释
- **命名规范**: 使用有意义的变量名

#### 8.1.2 代码审查
- **强制审查**: 所有代码必须经过审查
- **审查清单**: 功能正确性、性能、可读性
- **工具支持**: 使用静态分析工具

#### 8.1.3 静态分析
```bash
# Go静态分析工具
go vet          # 潜在错误检查
golangci-lint   # 综合检查
go fmt          # 代码格式化
```

### 8.2 文档质量

#### 8.2.1 必要文档
- [ ] API接口文档 (使用Swagger)
- [ ] 架构设计文档
- [ ] 部署运维手册
- [ ] 用户使用手册
- [ ] 开发者指南

#### 8.2.2 文档规范
- 使用Markdown格式
- 包含完整示例
- 定期更新维护
- 版本化管理

### 8.3 监控质量

#### 8.3.1 性能监控
- 响应时间监控
- 吞吐量监控
- 错误率监控
- 资源使用监控

#### 8.3.2 日志监控
- 异常日志实时告警
- 错误趋势分析
- 用户行为分析

## 9. 项目成功标准

### 9.1 功能标准
- [ ] 100%实现需求文档中的功能
- [ ] 通过所有功能测试用例
- [ ] Web界面完整可用
- [ ] 文档完整齐全

### 9.2 性能标准
- [ ] 支持50个Agent同时在线
- [ ] 文件扫描速度 > 100文件/分钟
- [ ] 上传成功率 > 99%
- [ ] 系统响应时间 < 2秒

### 9.3 质量标准
- [ ] 代码覆盖率 > 80%
- [ ] 无严重安全漏洞
- [ ] 无内存泄漏
- [ ] 稳定运行72小时

### 9.4 交付标准
- [ ] 完成部署文档
- [ ] 完成用户手册
- [ ] 完成培训材料
- [ ] 通过用户验收

---

**文档版本**: v1.0
**创建日期**: 2025-12-15
**最后更新**: 2025-12-15
**作者**: Claude Code
