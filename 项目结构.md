# Windows文档扫描与上传系统 - 项目结构

## 1. 整体项目结构

```
doc-scanner-system/
│
├── agent/                          # Windows Agent (Go)
│   ├── cmd/                        # 程序入口
│   │   └── main.go                 # Agent主程序入口
│   │
│   ├── internal/                   # 内部包
│   │   ├── config/                 # 配置管理
│   │   │   ├── config.go           # 配置结构体
│   │   │   ├── loader.go           # 配置加载器
│   │   │   └── encryptor.go        # 配置加密
│   │   │
│   │   ├── scanner/                # 文件扫描模块
│   │   │   ├── engine.go           # 扫描引擎
│   │   │   ├── filter.go           # 文件过滤器
│   │   │   └── fileinfo.go         # 文件信息
│   │   │
│   │   ├── uploader/               # 文件上传模块
│   │   │   ├── sftp_client.go      # SFTP客户端
│   │   │   ├── transfer.go         # 文件传输
│   │   │   ├── compressor.go       # 压缩模块
│   │   │   └── resume.go           # 断点续传
│   │   │
│   │   ├── heartbeat/              # 心跳监控模块
│   │   │   ├── service.go          # 心跳服务
│   │   │   ├── reporter.go         # 状态上报
│   │   │   └── sysinfo.go          # 系统信息收集
│   │   │
│   │   ├── service/                # 系统服务
│   │   │   ├── windows_service.go  # Windows服务封装
│   │   │   └── tray.go             # 系统托盘
│   │   │
│   │   └── logger/                 # 日志模块
│   │       ├── logger.go           # 日志封装
│   │       └── rotator.go          # 日志轮转
│   │
│   ├── pkg/                        # 公共包
│   │   ├── utils/                  # 工具函数
│   │   └── types/                  # 公共类型定义
│   │
│   ├── configs/                    # 配置文件
│   │   ├── default.json            # 默认配置
│   │   └── config.schema.json      # 配置Schema
│   │
│   ├── scripts/                    # 脚本文件
│   │   ├── build.bat               # Windows构建脚本
│   │   ├── install.bat             # 安装脚本
│   │   └── uninstall.bat           # 卸载脚本
│   │
│   ├── go.mod                      # Go模块文件
│   ├── go.sum                      # 依赖校验
│   ├── Makefile                    # 构建文件
│   └── README.md                   # Agent说明文档
│
├── server/                         # Server端 (Go + Gin)
│   ├── cmd/                        # 程序入口
│   │   └── main.go                 # Server主程序入口
│   │
│   ├── internal/                   # 内部包
│   │   ├── api/                    # API路由
│   │   │   ├── handlers/           # 处理器
│   │   │   │   ├── agent.go        # Agent相关接口
│   │   │   │   ├── config.go       # 配置相关接口
│   │   │   │   ├── file.go         # 文件相关接口
│   │   │   │   └── report.go       # 报告相关接口
│   │   │   │
│   │   │   ├── middleware/         # 中间件
│   │   │   │   ├── auth.go         # 认证中间件
│   │   │   │   ├── cors.go         # 跨域中间件
│   │   │   │   ├── logging.go      # 日志中间件
│   │   │   │   └── rate_limit.go   # 限流中间件
│   │   │   │
│   │   │   └── router.go           # 路由配置
│   │   │
│   │   ├── service/                # 业务逻辑层
│   │   │   ├── agent_service.go    # Agent服务
│   │   │   ├── config_service.go   # 配置服务
│   │   │   ├── file_service.go     # 文件服务
│   │   │   └── report_service.go   # 报告服务
│   │   │
│   │   ├── repository/             # 数据访问层
│   │   │   ├── agent_repo.go       # Agent数据访问
│   │   │   ├── config_repo.go      # 配置数据访问
│   │   │   ├── file_repo.go        # 文件数据访问
│   │   │   └── db.go               # 数据库连接
│   │   │
│   │   ├── model/                  # 数据模型
│   │   │   ├── agent.go            # Agent模型
│   │   │   ├── config.go           # 配置模型
│   │   │   ├── file.go             # 文件模型
│   │   │   └── common.go           # 公共模型
│   │   │
│   │   └── sftp/                   # SFTP相关
│   │       ├── client.go           # SFTP客户端池
│   │       └── uploader.go         # SFTP上传器
│   │
│   ├── web/                        # Web前端
│   │   ├── static/                 # 静态资源
│   │   │   ├── css/                # 样式文件
│   │   │   ├── js/                 # JavaScript文件
│   │   │   └── images/             # 图片资源
│   │   │
│   │   └── templates/              # HTML模板
│   │       ├── dashboard.html      # 仪表板
│   │       ├── agents.html         # Agent管理
│   │       ├── configs.html        # 配置管理
│   │       └── files.html          # 文件管理
│   │
│   ├── configs/                    # 配置文件
│   │   ├── server.yaml             # Server配置
│   │   └── database.yaml           # 数据库配置
│   │
│   ├── migrations/                 # 数据库迁移
│   │   ├── 001_init.sql            # 初始化脚本
│   │   └── 002_add_indexes.sql     # 索引脚本
│   │
│   ├── scripts/                    # 脚本文件
│   │   ├── build.sh                # Linux构建脚本
│   │   ├── build.bat               # Windows构建脚本
│   │   ├── docker-build.sh         # Docker构建脚本
│   │   └── migrate.sh              # 数据库迁移脚本
│   │
│   ├── deployments/                # 部署文件
│   │   ├── docker/                 # Docker部署
│   │   │   ├── Dockerfile          # Docker镜像
│   │   │   ├── docker-compose.yml  # 编排文件
│   │   │   └── nginx.conf          # Nginx配置
│   │   │
│   │   └── systemd/                # 系统服务
│   │       └── doc-scanner.service # 服务文件
│   │
│   ├── go.mod                      # Go模块文件
│   ├── go.sum                      # 依赖校验
│   ├── Makefile                    # 构建文件
│   └── README.md                   # Server说明文档
│
├── docs/                           # 项目文档
│   ├── api/                        # API文档
│   │   ├── openapi.yaml            # OpenAPI规范
│   │   └── postman/                # Postman测试集合
│   │
│   ├── architecture/               # 架构文档
│   │   ├── design.md               # 设计文档
│   │   └── database.md             # 数据库设计
│   │
│   ├── deployment/                 # 部署文档
│   │   ├── install-guide.md        # 安装指南
│   │   ├── config-guide.md         # 配置指南
│   │   └── ops-guide.md            # 运维指南
│   │
│   └── user/                       # 用户文档
│       ├── user-manual.md          # 用户手册
│       └── faq.md                  # 常见问题
│
├── tests/                          # 测试文件
│   ├── agent/                      # Agent测试
│   │   ├── unit/                   # 单元测试
│   │   ├── integration/            # 集成测试
│   │   └── e2e/                    # 端到端测试
│   │
│   └── server/                     # Server测试
│       ├── unit/                   # 单元测试
│       ├── integration/            # 集成测试
│       └── performance/            # 性能测试
│
├── scripts/                        # 公共脚本
│   ├── setup-dev.sh                # 开发环境搭建
│   ├── test-all.sh                 # 运行所有测试
│   ├── lint.sh                     # 代码检查
│   └── coverage.sh                 # 覆盖率检查
│
├── .gitignore                      # Git忽略文件
├── .golangci.yml                   # Go代码检查配置
├── .editorconfig                   # 编辑器配置
├── README.md                       # 项目说明文档
├── CHANGELOG.md                    # 变更日志
└── LICENSE                         # 开源协议
```

## 2. 核心模块说明

### 2.1 Agent模块

#### 2.1.1 配置管理 (internal/config)
- **config.go**: 定义配置结构体，包括扫描路径、SFTP配置、心跳间隔等
- **loader.go**: 配置文件加载器，支持JSON/YAML格式，支持热更新
- **encryptor.go**: 配置加密模块，用于加密存储敏感信息

#### 2.1.2 文件扫描 (internal/scanner)
- **engine.go**: 扫描引擎，遍历文件系统，识别文档文件
- **filter.go**: 文件过滤器，实现文件类型过滤、大小限制等规则
- **fileinfo.go**: 文件信息结构体，存储文件元数据

#### 2.1.3 文件上传 (internal/uploader)
- **sftp_client.go**: SFTP客户端，处理连接、认证等
- **transfer.go**: 文件传输核心逻辑，支持多文件并发上传
- **compressor.go**: 压缩模块，支持ZIP压缩
- **resume.go**: 断点续传实现，支持中断恢复

#### 2.1.4 心跳监控 (internal/heartbeat)
- **service.go**: 心跳服务，定时向Server发送心跳
- **reporter.go**: 状态上报，收集并上报Agent状态信息
- **sysinfo.go**: 系统信息收集，获取CPU、内存、磁盘使用情况

### 2.2 Server模块

#### 2.2.1 API层 (internal/api)
- **handlers/**: 各功能模块的HTTP处理器
  - agent.go: Agent注册、心跳、状态管理接口
  - config.go: 配置管理CRUD接口
  - file.go: 文件上传记录、查询接口
  - report.go: 统计报告、数据导出接口
- **middleware/**: HTTP中间件
  - auth.go: JWT认证中间件
  - cors.go: 跨域处理
  - logging.go: 请求日志记录
  - rate_limit.go: 接口限流
- **router.go**: 路由配置，整合所有路由和中间件

#### 2.2.2 业务逻辑层 (internal/service)
- **agent_service.go**: Agent业务逻辑，包括注册、状态更新、信息查询
- **config_service.go**: 配置业务逻辑，配置版本管理、分发策略
- **file_service.go**: 文件业务逻辑，上传记录统计、文件状态跟踪
- **report_service.go**: 报告业务逻辑，数据统计分析、报表生成

#### 2.2.3 数据访问层 (internal/repository)
- **agent_repo.go**: Agent数据访问，实现Agent的CRUD操作
- **config_repo.go**: 配置数据访问，配置版本控制、历史记录
- **file_repo.go**: 文件数据访问，上传记录查询、统计分析
- **db.go**: 数据库连接管理，连接池配置、事务处理

#### 2.2.4 数据模型 (internal/model)
- **agent.go**: Agent实体模型，定义Agent数据结构
- **config.go**: 配置模型，定义配置结构
- **file.go**: 文件模型，定义文件记录结构
- **common.go**: 公共模型，通用字段定义

## 3. 构建与部署

### 3.1 Agent构建

#### 构建命令
```bash
# 交叉编译 Windows x64
GOOS=windows GOARCH=amd64 go build -ldflags "-s -w" -o bin/agent.exe ./cmd/main.go

# 使用UPX压缩
upx --best bin/agent.exe

# 构建脚本
cd agent
./scripts/build.bat
```

#### 打包结构
```
agent-release/
├── agent.exe                    # 主程序
├── config.json                  # 默认配置文件
├── install.bat                  # 安装脚本
├── uninstall.bat                # 卸载脚本
├── readme.txt                   # 使用说明
└── uninstall.log                # 卸载日志
```

### 3.2 Server构建

#### 构建命令
```bash
# 构建Linux版本
GOOS=linux GOARCH=amd64 go build -ldflags "-s -w" -o bin/server ./cmd/main.go

# 构建Docker镜像
cd server
docker build -t doc-scanner-server:latest .

# 构建脚本
cd server
./scripts/build.sh
```

#### Docker部署
```bash
# 启动服务
docker-compose -f deployments/docker/docker-compose.yml up -d

# 查看日志
docker-compose -f deployments/docker/docker-compose.yml logs -f
```

## 4. 开发指南

### 4.1 环境准备

#### 开发工具
- Go 1.21+
- Visual Studio Code / GoLand
- Git
- Docker (可选)
- SQLiteBrowser (数据库管理)

#### 依赖管理
```bash
# Agent依赖
cd agent
go mod tidy
go mod vendor

# Server依赖
cd server
go mod tidy
go mod vendor
```

### 4.2 代码规范

#### Go代码规范
1. **命名规范**
   - 使用驼峰命名法
   - 包名使用小写字母
   - 私有方法以下划线开头
   - 常量使用全大写字母

2. **注释规范**
   - 公共API必须添加注释
   - 注释使用完整的句子
   - 以函数名开头，首字母大写

3. **错误处理**
   - 优先使用错误返回值
   - 自定义错误类型
   - 错误包装使用fmt.Errorf

#### 示例代码
```go
// 扫描文件并返回文件列表
func (e *Engine) ScanFiles(paths []string) ([]FileInfo, error) {
    var results []FileInfo
    for _, path := range paths {
        files, err := e.scanPath(path)
        if err != nil {
            return nil, fmt.Errorf("scan path %s failed: %w", path, err)
        }
        results = append(results, files...)
    }
    return results, nil
}
```

### 4.3 测试规范

#### 单元测试
```go
// 示例：文件扫描单元测试
func TestEngine_ScanFiles(t *testing.T) {
    tests := []struct {
        name    string
        paths   []string
        want    int
        wantErr bool
    }{
        {
            name:    "single file",
            paths:   []string{"testdata/sample.txt"},
            want:    1,
            wantErr: false,
        },
    }

    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            e := NewEngine()
            got, err := e.ScanFiles(tt.paths)
            if (err != nil) != tt.wantErr {
                t.Errorf("ScanFiles() error = %v, wantErr %v", err, tt.wantErr)
                return
            }
            if len(got) != tt.want {
                t.Errorf("ScanFiles() got = %v, want %v", len(got), tt.want)
            }
        })
    }
}
```

#### 基准测试
```go
// 示例：文件扫描性能测试
func BenchmarkEngine_ScanFiles(b *testing.B) {
    e := NewEngine()
    paths := []string{"testdata/large_dir"}

    b.ResetTimer()
    for i := 0; i < b.N; i++ {
        e.ScanFiles(paths)
    }
}
```

### 4.4 Git工作流

#### 分支策略
- **main**: 主分支，稳定的生产代码
- **develop**: 开发分支，集成最新功能
- **feature/xxx**: 功能分支，开发新功能
- **bugfix/xxx**: 修复分支，修复bug
- **release/x.y.z**: 发布分支，准备发布

#### 提交规范
```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type类型**:
- feat: 新功能
- fix: 修复bug
- docs: 文档更新
- style: 代码格式调整
- refactor: 重构代码
- test: 添加或修改测试
- chore: 构建或辅助工具变动

**示例**:
```
feat(scanner): 支持增量文件扫描

- 添加文件修改时间检查
- 实现基于时间戳的增量扫描
- 添加扫描进度显示

Closes #123
```

## 5. 常见问题

### 5.1 开发问题

**Q: Go模块依赖下载失败怎么办？**
A: 使用国内镜像源
```bash
go env -w GOPROXY=https://goproxy.cn,direct
```

**Q: Windows服务如何调试？**
A: 使用事件查看器查看日志，或在控制台模式运行
```bash
agent.exe --console
```

**Q: SFTP连接失败如何排查？**
A: 检查以下项目：
1. 网络连通性 (telnet host port)
2. 认证信息是否正确
3. SSH密钥配置
4. 文件系统权限

### 5.2 部署问题

**Q: Agent无法启动？**
A: 检查以下几点：
1. 是否有管理员权限
2. 防火墙是否阻止
3. 配置文件是否正确
4. 日志文件查看错误信息

**Q: Server响应慢？**
A: 优化建议：
1. 检查数据库索引
2. 调整连接池大小
3. 启用缓存机制
4. 使用Nginx反向代理

---

**文档版本**: v1.0
**创建日期**: 2025-12-15
**作者**: Claude Code
